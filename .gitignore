.venv/
.python/
.python.part/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.playwright-mcp/
.env

# The downloaded GTFS feed (~67 MB) and the local cache database, at the repo root only.
# Anchored with a leading slash: an unanchored `data/` would also swallow
# src/tripps/data/airports.json, which is packaged code, not a build artefact.
/data/

# serena MCP tooling
.serena/

# local dev-harness config (ports, run commands) — personal, not part of the project
.claude/

# built by ./build-app.sh
tripps.app/
